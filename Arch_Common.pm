#!/usr/bin/perl -w
#=======================================================================
package Arch_Common;

use strict;
use warnings;

our ($keymap_directory, $keymap_extension);

$keymap_directory = '/usr/share/kbd/keymaps';
$keymap_extension = '.map.gz';
1;